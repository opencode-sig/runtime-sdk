// Package gatewaymeta defines Gateway route metadata and protobuf descriptor publication helpers.
//
// Route specs must declare the explicit public Gateway path. The package
// normalizes leading/trailing slashes but does not add service-name prefixes.
package gatewaymeta
